import React from 'react';
import {redirect} from "next/navigation";

const Page = () => {
    redirect("/prospect/institutions")

    return (
        <div>
            asd
        </div>
    );
};

export default Page;